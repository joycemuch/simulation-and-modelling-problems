f0=1;
T=2;
phi=pi/4;
fs=100;
t=0:1/fs:T;

xt = -2*sin(2*pi*f0*t)+4*cos(pi*f0*t+phi);

length(xt)

max(xt)

min(xt)

sum(xt)

mean(xt)

var(xt)

t=0:1;
   xt = -2*sin(2*pi*f0*t)+4*cos(pi*f0*t+phi);
  var(xt)
  
t=0:51;
 xt = -2*sin(2*pi*f0*t)+4*cos(pi*f0*t+phi);
  var(xt)
  
  